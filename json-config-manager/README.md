# JSON Config Manager
   
   A Python script that reads a JSON configuration file and 
   validates it against a list of required keys, reporting 
   which keys are present or missing.
   
   ## What I learned
   - Reading and parsing JSON files with the json module
   - Using 'with open()' for safe file handling
   - Writing functions that return multiple values
   - Looping through data to validate required fields
